(()=>{"use strict";
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const KEY="halaqoh_professional_v3";let meUser=null;
const defaultDB={users:[{id:"admin",name:"Administrator",username:"admin",password:"admin123",role:"admin"}],students:[],groups:[],records:[]};
let db=load();
function load(){try{let x=JSON.parse(localStorage.getItem(KEY));return x?.users&&x?.students&&x?.groups&&x?.records?x:structuredClone(defaultDB)}catch{return structuredClone(defaultDB)}}
function save(){localStorage.setItem(KEY,JSON.stringify(db))}
function esc(v=""){return String(v).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}
function uid(p){return p+Date.now()+Math.random().toString(36).slice(2,6)}
function me(){return db.users.find(x=>x.id===meUser)}
function toast(msg,error=false){let t=$("#toast");t.textContent=msg;t.className="toast show";t.style.background=error?"#a52a21":"";clearTimeout(window.__toast);window.__toast=setTimeout(()=>t.className="toast",2500)}
function closeModal(){$("#modal").innerHTML=""}
function modal(title,body,fn){$("#modal").innerHTML=`<div class="modalbg" id="modalbg"><div class="modalcard"><div class="modalhead"><h2>${title}</h2><button class="icon-btn" type="button" data-close>×</button></div><form id="mf">${body}<div class="actions"><button type="button" class="btn" style="background:#eef5f2;color:#285b51" data-close>Batal</button><button class="btn primary">Simpan</button></div></form></div></div>`;let bg=$("#modalbg"),f=$("#mf");$$("[data-close]",bg).forEach(x=>x.onclick=closeModal);bg.onclick=e=>{if(e.target===bg)closeModal()};f.onsubmit=e=>{e.preventDefault();fn(f)}}
document.addEventListener("keydown",e=>{if(e.key==="Escape")closeModal()});
const surahs=["Al-Fatihah","Al-Baqarah","Ali 'Imran","An-Nisa'","Al-Ma'idah","Al-An'am","Al-A'raf","Al-Anfal","At-Taubah","Yunus","Hud","Yusuf","Ar-Ra'd","Ibrahim","Al-Hijr","An-Nahl","Al-Isra'","Al-Kahf","Maryam","Ta-Ha","Al-Anbiya'","Al-Hajj","Al-Mu'minun","An-Nur","Al-Furqan","Asy-Syu'ara'","An-Naml","Al-Qasas","Al-'Ankabut","Ar-Rum","Luqman","As-Sajdah","Al-Ahzab","Saba'","Fatir","Ya-Sin","As-Saffat","Sad","Az-Zumar","Ghafir","Fussilat","Asy-Syura","Az-Zukhruf","Ad-Dukhan","Al-Jasiyah","Al-Ahqaf","Muhammad","Al-Fath","Al-Hujurat","Qaf","Az-Zariyat","At-Tur","An-Najm","Al-Qamar","Ar-Rahman","Al-Waqi'ah","Al-Hadid","Al-Mujadilah","Al-Hasyr","Al-Mumtahanah","As-Saff","Al-Jumu'ah","Al-Munafiqun","At-Tagabun","At-Talaq","At-Tahrim","Al-Mulk","Al-Qalam","Al-Haqqah","Al-Ma'arij","Nuh","Al-Jinn","Al-Muzzammil","Al-Muddassir","Al-Qiyamah","Al-Insan","Al-Mursalat","An-Naba'","An-Nazi'at","'Abasa","At-Takwir","Al-Infitar","Al-Mutaffifin","Al-Insyiqaq","Al-Buruj","At-Tariq","Al-A'la","Al-Gasyiyah","Al-Fajr","Al-Balad","Asy-Syams","Al-Lail","Ad-Duha","Asy-Syarh","At-Tin","Al-'Alaq","Al-Qadr","Al-Bayyinah","Az-Zalzalah","Al-'Adiyat","Al-Qari'ah","At-Takatsur","Al-'Asr","Al-Humazah","Al-Fil","Quraisy","Al-Ma'un","Al-Kausar","Al-Kafirun","An-Nasr","Al-Lahab","Al-Ikhlas","Al-Falaq","An-Nas"];
function visible(){return me().role==="admin"?db.students:db.students.filter(s=>s.ustadzId===me().id)}
function login(){let u=$("#username").value.trim(),p=$("#password").value,f=db.users.find(x=>x.username===u&&x.password===p);if(!f){$("#loginError").textContent="Username atau password salah.";$("#loginError").classList.remove("hidden");return}meUser=f.id;sessionStorage.setItem(KEY,meUser);start()}
function start(){$("#login").classList.add("hidden");$("#app").classList.remove("hidden");render()}
$("#loginForm").onsubmit=e=>{e.preventDefault();login()};$("#togglePassword").onclick=()=>{$("#password").type=$("#password").type==="password"?"text":"password";$("#togglePassword").textContent=$("#password").type==="password"?"Lihat":"Sembunyikan"};$("#logout").onclick=()=>{meUser=null;sessionStorage.removeItem(KEY);$("#app").classList.add("hidden");$("#login").classList.remove("hidden");$("#password").value=""};$("#menu").onclick=()=>$("#sidebar").classList.toggle("open");
function nav(){let admin=me().role==="admin",items=admin?[["dashboard","⌂","Dashboard"],["students","♙","Santri"],["groups","◈","Kelompok"],["users","♟","Akun Ustadz"],["records","✎","Setoran"],["reports","▣","Rekap"]]:[["dashboard","⌂","Dashboard"],["students","♙","Santri Binaan"],["records","✎","Setoran"],["reports","▣","Rekap"]],p=location.hash.slice(1)||"dashboard";$("#nav").innerHTML=items.map(i=>`<button class="nav-item ${p===i[0]?"active":""}" data-p="${i[0]}"><span class="nav-icon">${i[1]}</span>${i[2]}</button>`).join("");$$(".nav-item").forEach(b=>b.onclick=()=>{location.hash=b.dataset.p;$("#sidebar").classList.remove("open")});let u=me(),initial=(u.name||"U")[0].toUpperCase();$("#who-name").textContent=u.name;$("#who-role").textContent=admin?"Administrator":"Ustadz";$("#avatar").textContent=initial;$("#top-name").textContent=u.name;$("#top-role").textContent=admin?"Administrator":"Ustadz";$("#top-avatar").textContent=initial;$("#top-date").textContent=new Date().toLocaleDateString("id-ID",{weekday:"long",day:"numeric",month:"long",year:"numeric"})}
function head(t,s,a=""){return `<div class="page-head"><div><span class="eyebrow">HALAQOH TAHFIDZ</span><h1>${t}</h1><p>${s}</p></div><div class="head-actions">${a}</div></div>`}
function stat(icon,name,val,sub){return `<div class="stat"><div class="stat-icon">${icon}</div><span>${name}</span><b>${val}</b><small>${sub}</small></div>`}
function dashboard(){let ss=visible(),rs=db.records.filter(r=>ss.some(s=>s.id===r.studentId)),z=rs.filter(r=>r.type==="Ziyadah").length,m=rs.filter(r=>r.type==="Murajaah").length,av=rs.length?(rs.reduce((a,r)=>a+r.score,0)/rs.length).toFixed(1):"—",recent=[...rs].sort((a,b)=>b.date.localeCompare(a.date)).slice(0,5);return head("Dashboard","Ringkasan perkembangan halaqoh Anda.")+`<div class="stats">${stat("♙","Santri",ss.length,"santri terdaftar")}${stat("✎","Total Setoran",rs.length,"catatan hafalan")}${stat("↗","Ziyadah",z,"setoran baru")}${stat("↻","Murajaah",m,"pengulangan hafalan")}</div><div class="grid2"><section class="panel"><div class="panel-title"><h2>Setoran terbaru</h2><button class="text-btn" onclick="location.hash='records'">Lihat semua →</button></div>${recent.map(r=>{let s=db.students.find(x=>x.id===r.studentId);return `<div class="list-row"><div class="avatar">${esc((s?.name||"?")[0])}</div><div style="flex:1"><b>${esc(s?.name||"—")}</b><small>${esc(r.type)} · ${esc(r.fromSurah)} ${esc(r.fromAyat)} → ${esc(r.toSurah)} ${esc(r.toAyat)}</small></div><strong class="score">${r.score}</strong></div>`}).join("")||`<div class="empty">Belum ada setoran. Mulai catat hafalan santri.</div>`}</section><section class="panel"><div class="panel-title"><h2>Ringkasan nilai</h2></div><div class="list-row"><span>Rata-rata nilai</span><b class="score">${av}</b></div><div class="list-row"><span>Sangat baik (90+)</span><b>${rs.filter(r=>r.score>=90).length}</b></div><div class="list-row"><span>Perlu murajaah (&lt;80)</span><b>${rs.filter(r=>r.score<80).length}</b></div><div class="quick" style="margin-top:12px"><button onclick="recordModal()">＋ Catat Setoran Baru</button></div></section></div>`}
function students(){let a=me().role==="admin",ss=visible();return head(a?"Data Santri":"Santri Binaan",a?"Kelola data santri dan pengelompokan.":"Daftar santri yang dibina oleh Anda.",a?`<button class="btn primary" onclick="studentModal()">＋ Tambah Santri</button>`:"")+`<section class="panel"><div class="table-wrap"><table><thead><tr><th>Nama Santri</th><th>Kelas</th><th>Kelompok</th><th>Setoran</th>${a?"<th>Aksi</th>":""}</tr></thead><tbody>${ss.map(s=>{let g=db.groups.find(g=>g.id===s.groupId);return `<tr><td><b>${esc(s.name)}</b></td><td>${esc(s.className||"—")}</td><td>${esc(g?.name||"Belum dikelompokkan")}</td><td>${db.records.filter(r=>r.studentId===s.id).length}</td>${a?`<td><button class="btn" style="padding:7px 10px;background:#fff1ef;color:#a52a21" onclick="delStudent('${s.id}')">Hapus</button></td>`:""}</tr>`}).join("")||`<tr><td colspan="${a?5:4}" class="empty">Belum ada santri.</td></tr>`}</tbody></table></div></section>`}
function groups(){return head("Kelompok Halaqoh","Atur kelompok dan ustadz pembina.",`<button class="btn primary" onclick="groupModal()">＋ Buat Kelompok</button>`)+`<section class="cards">${db.groups.map(g=>`<div class="card"><span class="eyebrow">HALAQOH</span><h3>${esc(g.name)}</h3><p>Ustadz pembina<br><b style="color:var(--ink)">${esc(db.users.find(u=>u.id===g.ustadzId)?.name||"Belum ditentukan")}</b></p><b>${db.students.filter(s=>s.groupId===g.id).length} santri</b></div>`).join("")||`<div class="empty">Belum ada kelompok.</div>`}</section>`}
function users(){let us=db.users.filter(u=>u.role==="ustadz");return head("Akun Ustadz","Kelola akun pembina halaqoh.",`<button class="btn primary" onclick="userModal()">＋ Buat Akun</button>`)+`<section class="cards">${us.map(u=>`<div class="card"><div class="avatar" style="margin-bottom:12px">${esc((u.name||"U")[0])}</div><h3>${esc(u.name)}</h3><p>@${esc(u.username)}</p><button class="btn" style="background:#fff1ef;color:#a52a21" onclick="delUser('${u.id}')">Hapus akun</button></div>`).join("")||`<div class="empty">Belum ada akun ustadz.</div>`}</section>`}
function recordModal(){let ss=visible();if(!ss.length)return toast("Belum ada santri. Tambahkan santri terlebih dahulu.",true);let opts=surahs.map((s,i)=>`<option value="${i}">${i+1}. ${s}</option>`).join("");modal("Catat Setoran Hafalan",`<div class="formgrid"><label>Santri<select name="studentId">${ss.map(s=>`<option value="${s.id}">${esc(s.name)}</option>`).join("")}</select></label><label>Jenis Setoran<select name="type"><option value="Ziyadah">📖 Ziyadah — Hafalan baru</option><option value="Murajaah">🔄 Murajaah — Mengulang hafalan</option></select></label><label>Dari Surat<select name="from">${opts}</select></label><label>Dari Ayat<input name="fromAyat" required placeholder="Contoh: 1"></label><label>Sampai Surat<select name="to">${opts}</select></label><label>Sampai Ayat<input name="toAyat" required placeholder="Contoh: 10"></label><label>Tanggal<input name="date" type="date" value="${new Date().toISOString().slice(0,10)}" required></label><label>Nilai<select name="score"><option value="90">90 — Sangat Baik</option><option value="80">80 — Baik</option><option value="70">70 — Cukup</option><option value="60">60 — Perlu Murajaah</option></select></label><label>Catatan<input name="note" placeholder="Catatan ustadz"></label></div>`,f=>{let fi=+f.from.value,ti=+f.to.value;if(ti<fi)return toast("Surat sampai tidak boleh sebelum surat dari.",true);db.records.push({id:uid("r"),studentId:f.studentId.value,type:f.type.value,fromSurah:surahs[fi],fromAyat:f.fromAyat.value.trim(),toSurah:surahs[ti],toAyat:f.toAyat.value.trim(),date:f.date.value,score:+f.score.value,note:f.note.value.trim()});save();closeModal();toast("Setoran berhasil disimpan");render()})}
function records(){let ss=visible(),rs=db.records.filter(r=>ss.some(s=>s.id===r.studentId)).sort((a,b)=>b.date.localeCompare(a.date));return head("Setoran Hafalan","Pantau ziyadah dan murajaah secara terstruktur.",`<button class="btn primary" onclick="recordModal()">＋ Catat Setoran</button>`)+`<section class="panel"><div class="table-wrap"><table><thead><tr><th>Tanggal</th><th>Santri</th><th>Jenis</th><th>Rentang Hafalan</th><th>Nilai</th><th>Catatan</th></tr></thead><tbody>${rs.map(r=>`<tr><td>${r.date}</td><td><b>${esc(ss.find(s=>s.id===r.studentId)?.name||"—")}</b></td><td><span style="color:${r.type==="Ziyadah"?"var(--green)":"#a87522"};font-weight:800">${esc(r.type)}</span></td><td>${esc(r.fromSurah)} ${esc(r.fromAyat)} <b>→</b> ${esc(r.toSurah)} ${esc(r.toAyat)}</td><td class="score">${r.score}</td><td>${esc(r.note||"—")}</td></tr>`).join("")||`<tr><td colspan="6" class="empty">Belum ada setoran.</td></tr>`}</tbody></table></div></section>`}
function reports(){let ss=visible(),rs=db.records.filter(r=>ss.some(s=>s.id===r.studentId));return head("Rekap Perkembangan","Laporan ringkas dan siap dicetak.",`<button class="btn primary" onclick="pdf()">↓ Unduh PDF</button>`)+`<section class="panel"><div class="report-summary"><div><span>Total Santri</span><b>${ss.length}</b></div><div><span>Total Setoran</span><b>${rs.length}</b></div><div><span>Ziyadah</span><b>${rs.filter(r=>r.type==="Ziyadah").length}</b></div><div><span>Murajaah</span><b>${rs.filter(r=>r.type==="Murajaah").length}</b></div></div><div class="table-wrap"><table><thead><tr><th>Santri</th><th>Ziyadah</th><th>Murajaah</th><th>Rata-rata</th><th>Hafalan terakhir</th></tr></thead><tbody>${ss.map(s=>{let x=rs.filter(r=>r.studentId===s.id),z=x.filter(r=>r.type==="Ziyadah").length,m=x.filter(r=>r.type==="Murajaah").length,av=x.length?(x.reduce((a,r)=>a+r.score,0)/x.length).toFixed(1):"—",last=x[x.length-1];return `<tr><td><b>${esc(s.name)}</b></td><td>${z}</td><td>${m}</td><td class="score">${av}</td><td>${last?esc(last.fromSurah)+" "+esc(last.fromAyat)+" → "+esc(last.toSurah)+" "+esc(last.toAyat):"—"}</td></tr>`}).join("")||`<tr><td colspan="5" class="empty">Belum ada data.</td></tr>`}</tbody></table></div></section>`}
function studentModal(){let gs=db.groups.map(g=>`<option value="${g.id}">${esc(g.name)}</option>`).join("");modal("Tambah Santri",`<div class="formgrid"><label>Nama Lengkap<input name="name" required placeholder="Nama santri"></label><label>Kelas<input name="className" placeholder="Contoh: 8A"></label><label>Kelompok<select name="groupId"><option value="">Belum dikelompokkan</option>${gs}</select></label></div>`,f=>{let g=db.groups.find(x=>x.id===f.groupId.value);db.students.push({id:uid("s"),name:f.name.value.trim(),className:f.className.value.trim(),groupId:g?.id||"",ustadzId:g?.ustadzId||""});save();closeModal();toast("Santri berhasil ditambahkan");render()})}
function userModal(){modal("Buat Akun Ustadz",`<div class="formgrid"><label>Nama Lengkap<input name="name" required></label><label>Username<input name="username" required></label><label>Password<input name="password" type="password" required></label></div>`,f=>{let u=f.username.value.trim();if(db.users.some(x=>x.username===u))return toast("Username sudah digunakan.",true);db.users.push({id:uid("u"),name:f.name.value.trim(),username:u,password:f.password.value,role:"ustadz"});save();closeModal();toast("Akun ustadz berhasil dibuat");render()})}
function groupModal(){let us=db.users.filter(u=>u.role==="ustadz");if(!us.length)return toast("Buat akun ustadz terlebih dahulu.",true);modal("Buat Kelompok Halaqoh",`<div class="formgrid"><label>Nama Kelompok<input name="name" required placeholder="Halaqoh Al-Fatihah"></label><label>Ustadz Pembina<select name="ustadzId">${us.map(u=>`<option value="${u.id}">${esc(u.name)}</option>`).join("")}</select></label></div>`,f=>{db.groups.push({id:uid("g"),name:f.name.value.trim(),ustadzId:f.ustadzId.value});save();closeModal();toast("Kelompok berhasil dibuat");render()})}
function delStudent(id){if(confirm("Hapus santri beserta seluruh setoran terkait?")){db.students=db.students.filter(s=>s.id!==id);db.records=db.records.filter(r=>r.studentId!==id);save();render()}}
function delUser(id){if(confirm("Hapus akun ustadz ini?")){db.users=db.users.filter(u=>u.id!==id);db.groups=db.groups.map(g=>g.ustadzId===id?{...g,ustadzId:""}:g);db.students=db.students.map(s=>s.ustadzId===id?{...s,ustadzId:""}:s);save();render()}}
function pdf(){if(!window.jspdf?.jsPDF)return toast("Library PDF belum siap. Periksa koneksi internet.",true);let {jsPDF}=window.jspdf,doc=new jsPDF({unit:"mm",format:"a4"}),ss=visible(),rs=db.records.filter(r=>ss.some(s=>s.id===r.studentId));doc.setFillColor(7,61,53);doc.rect(0,0,210,35,"F");doc.setTextColor(255,255,255);doc.setFontSize(18);doc.text("HALAQOH TAHFIDZ ONLINE",15,14);doc.setFontSize(9);doc.text("REKAP PERKEMBANGAN HAFALAN SANTRI",15,23);doc.setTextColor(45,72,65);doc.setFontSize(10);doc.text("Pengelola : "+me().name,15,46);doc.text("Tanggal : "+new Date().toLocaleDateString("id-ID"),15,53);doc.text("Santri : "+ss.length+"   Setoran : "+rs.length+"   Ziyadah : "+rs.filter(r=>r.type==="Ziyadah").length+"   Murajaah : "+rs.filter(r=>r.type==="Murajaah").length,15,60);let y=73;doc.setFillColor(234,247,242);doc.rect(10,y-6,190,10,"F");doc.setTextColor(20,75,65);doc.setFontSize(8);["No","Nama Santri","Jenis","Dari","Sampai","Nilai"].forEach((x,i)=>doc.text(x,[13,25,75,96,145,190][i],y));y+=8;rs.forEach((r,i)=>{if(y>278){doc.addPage();y=18}let n=ss.find(s=>s.id===r.studentId)?.name||"—";doc.setTextColor(45,65,60);doc.text(String(i+1),13,y);doc.text(n.slice(0,23),25,y);doc.text(r.type,75,y);doc.text((r.fromSurah+" "+r.fromAyat).slice(0,25),96,y);doc.text((r.toSurah+" "+r.toAyat).slice(0,25),145,y);doc.text(String(r.score),190,y);y+=6.5});doc.setTextColor(120,135,130);doc.setFontSize(7);doc.text("Dokumen dibuat oleh Aplikasi Halaqoh Tahfidz Online",15,289);doc.save("Rekap-Halaqoh-"+new Date().toISOString().slice(0,10)+".pdf");toast("PDF berhasil dibuat")}
function render(){if(!meUser)return;nav();let a=me().role==="admin",p=location.hash.slice(1)||"dashboard",pages={dashboard,students,groups,users,records,reports},allowed=a?Object.keys(pages):["dashboard","students","records","reports"];$("#view").innerHTML=pages[allowed.includes(p)?p:"dashboard"]()}
window.addEventListener("hashchange",render);Object.assign(window,{studentModal,userModal,groupModal,recordModal,delStudent,delUser,pdf,closeModal});
let saved=sessionStorage.getItem(KEY);if(saved&&db.users.some(u=>u.id===saved)){meUser=saved;start()}
})();